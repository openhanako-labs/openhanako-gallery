# Hanako 图库插件 · 设计文档

> 版本：v0.2 · 开发阶段（Phase 0~1 已完成）
> 插件 ID：`hanako-gallery`
> 对应博客：XHBlogs（Next.js + Vercel）

---

## 一、定位与边界

### 核心定位

一个 Hanako 插件，管理博客写作所需的图片——**收进来、管得住、用得上**。

不是手机照片管家，不是全盘图片扫描器，不是图床替代品。

### 边界

| 做 | 不做 |
|---|---|
| 本地目录扫描入库 | 手机端投递（初期） |
| EXIF 读取 + 自动分类 | 图片编辑/裁剪 |
| 标签系统（多对多） | AI 自动打标（初期） |
| 搜索（文件名/日期/标签） | 人脸识别 |
| HTML 画廊页生成 | 无限滚动流 |
| Markdown 引用代码生成 | 云同步 |
| 缩略图按需生成 | 批量去重（已内置） |
| 索引导出/备份 | 外链 URL 抓取 |

### 与博客的关系

```
手机/电脑 → 微信/复制 → 图库扫描目录 → SQLite 索引 → 博客引用
                                              ↓
                                       助手搜图/选图 → Markdown 引用
```

图库不接管博客的图床上传，不替代 Git 部署。它的角色是：**让助手能帮你找到、选出、引用博客需要的图片**。

### 使用场景

**场景一：写博客找配图**

> 打开助手 → "帮我在图库里找一张京都的夜景照片" → 助手搜出结果 → "这张不错，给我生成 Markdown 引用" → 粘贴到博客文章里

**场景二：批量导入新照片**

> 手机拍了一组照片 → 传到电脑的一个暂存文件夹 → "小曦，帮我把导入目录里新来的图扫一下" → 图库自动分类入库

**场景三：按标签挑图发系列**

> "列出所有打了'折纸'标签的图片" → 画廊页展示 → 选几张做折纸教程的配图

这三个场景贯穿了插件的三个核心能力：**搜得到、管得住、用得上**。

---

## 二、进展状态

### ✅ Phase 0 · 骨架（已完成）
插件架子搭建完毕，工具注册 + 配置系统就位。
交付验证：
- `gallery_ping` — 心跳检测，返回版本、配置、数据库连接状态 ✅
- 6 个配置项 schema 注册（`gallery.galleryRoot` 等） ✅
- 插件注册到 Hanako，dev 模式加载通过 ✅

### ✅ Phase 1 · 收得进来（已完成）
图片从指定目录进库，可搜索。
交付验证：
- `gallery_rebuild` — 扫描图库目录，读取 EXIF，重建 SQLite 索引 ✅
- `gallery_search` — 按文件名/日期/标签搜索，返回缩略图 ✅
- `gallery_tag` — 标签增删改查（含合并/重命名） ✅
- `gallery_config` — 查看配置 ✅
- 缩略图按需生成（sharp + EXIF Orientation 自动旋转） ✅
- SHA-256 去重（入库时自动检查） ✅

### 🟡 Phase 2 · 用得上（进行中）
图库的内容能被博客消费。
交付：
- ✅ `gallery_markdown` — 生成 Markdown 图片引用代码
- ✅ `gallery_generate` — 生成 HTML 画廊页（代码已就绪，待注册）
  - 按日期分组 + 响应式网格
  - 标签筛选
  - 点击查看大图

### ✅ Phase 3 · 好打理（已完成）
让图库长期用着舒服。
交付：
- 增量扫描（由 gallery_rebuild 实现——扫描图库目录，SHA-256 去重）
- 批量操作（gallery_tag image_ids 数组支持批量）
- 标签管理增强（rename/delete 已集成到 gallery_tag）
- ✅ `gallery_export` — 索引导出（JSON 结构化数据 / SQLite 文件拷贝）

### ✅ Phase 4 · 路径对齐（已完成）
博客路径与图库输出路径对齐。
交付：
- ✅ 博客 `public/images/gallery/` 目录已创建
- ✅ `blogImagesPath` 默认值 `public/images/gallery`（文件系统路径），Markdown 引用自动转为 `/images/gallery/...`（Web 路径）
- ✅ `gallery_sync` — 拷贝图库索引到博客目录（确保路径一致）

---

## 三、数据架构

### 图片本体存储

```
gallery/
├── 2025/                     # EXIF 日期有值 → 按年
│   ├── 03/                   # 按月
│   │   ├── 15/               # 按日
│   │   │   ├── IMG_2847.jpg  # 原图
│   │   │   └── DSC_0041.png
│   │   └── 20/
│   └── 05/
├── _uncategorized/           # EXIF 无日期兜底
├── _thumbnails/              # 缩略图缓存（按需生成）
└── _index.db                 # SQLite 索引数据库（sql.js WASM 实现）
```

- `gallery/` 根路径可配置（默认 `D:\Pictures\gallery`）
- `_uncategorized/` 放无 EXIF 或日期解析失败的图片
- `_thumbnails/` 结构与原图目录平行，如 `_thumbnails/2025/03/15/IMG_2847.jpg`
- 缩略图生成时自动处理 EXIF Orientation（sharp.rotate()），手机照片自动转正
- `_index.db` 是 sql.js 实现的单文件数据库

### SQLite 表结构

```sql
-- 图片主表
CREATE TABLE images (
    id             TEXT PRIMARY KEY,     -- UUID v4
    file_hash      TEXT NOT NULL UNIQUE, -- SHA-256 文件哈希（去重用）
    path           TEXT NOT NULL,        -- gallery/ 下的相对路径
    filename       TEXT NOT NULL,
    ext            TEXT NOT NULL,        -- jpg / png / webp / gif
    size_bytes     INTEGER,
    width          INTEGER,
    height         INTEGER,
    date_taken     TEXT,                 -- EXIF DateTimeOriginal (ISO 8601), 可能为空
    date_imported  TEXT NOT NULL,        -- 入库时间
    date_modified  TEXT,                 -- 文件修改时间（EXIF 降级用）
    camera_make    TEXT,
    camera_model   TEXT,
    thumbnail_path TEXT,                 -- 缩略图相对路径，为空表示未生成
    hidden         INTEGER DEFAULT 0     -- 软删除/隐藏
);

-- 索引
CREATE INDEX idx_date_taken ON images(date_taken);
CREATE INDEX idx_date_imported ON images(date_imported);
CREATE INDEX idx_ext ON images(ext);

-- 标签表（多对多）
CREATE TABLE tags (
    id    INTEGER PRIMARY KEY AUTOINCREMENT,
    name  TEXT NOT NULL UNIQUE
);

-- 图片-标签关联表
CREATE TABLE image_tags (
    image_id TEXT NOT NULL REFERENCES images(id),
    tag_id   INTEGER NOT NULL REFERENCES tags(id),
    PRIMARY KEY (image_id, tag_id)
);

CREATE INDEX idx_image_tags_tag ON image_tags(tag_id);
```

### 为什么选 sql.js（WASM SQLite）

选型变更为 sql.js（而非 better-sqlite3）的原因：
- **编译依赖**：better-sqlite3 需要 node-gyp + CL.exe（C++ 编译器），当前 Windows 环境缺失，无法编译
- **零依赖部署**：sql.js 是纯 WASM，`npm install` 即装即用
- **够用的性能**：配合防抖写入（debouncedSave）策略，批量操作只触发一次全量序列化

代价：
- 每次写入后全量序列化回写文件（`export()` + `writeFileSync`），大库（>5000 张）性能会下降
- 不支持 WAL 模式、`PRAGMA` 特性受限
- 数据在内存中，断电丢失内存中未持久化的变更（防抖延迟期内）

---

## 四、插件结构

```
hanako-gallery/
├── manifest.json              # 插件元数据（工具注册 + 配置 schema + 页面路由）
├── index.js                   # 插件生命周期入口（目录预热）
├── package.json
├── lib/
│   ├── db.js                  # SQLite 初始化 + 查询封装（sql.js WASM）
│   ├── scanner.js             # 文件扫描 + EXIF 提取 + 入库（Phase 1）
│   ├── tagger.js              # 标签 CRUD（Phase 1）
│   └── thumbnail.js           # 缩略图按需生成（sharp + EXIF 自动旋转）
├── tools/                     # Hanako 工具
│   ├── ping.js                # 心跳 + 数据库状态
│   ├── rebuild.js             # 重建索引
│   ├── import.js              # 导入新图片
│   ├── search.js              # 搜索图片 + 单张详情
│   ├── tag.js                 # 标签管理
│   ├── config.js              # 查看/修改配置
│   ├── markdown.js            # 生成 Markdown 引用（Phase 2）
│   ├── generate.js            # 生成 HTML 画廊页（Phase 2）
│   ├── export.js              # 索引导出（Phase 3）
│   └── sync.js                # 同步到博客目录（Phase 4）
├── skills/
│   └── gallery-guide/
│       └── SKILL.md           # 助手使用引导
├── pages/                     # Web UI 页面
│   └── gallery.html           # 图库画廊页（搜索+标签+详情）
```

---

## 五、工具清单（按阶段）

### Phase 1 工具（已交付）

| 工具名 | 功能 | 参数 |
|--------|------|------|
| `gallery_ping` | 心跳检测 + DB 状态+ 配置 | 无 |
| `gallery_rebuild` | 重建索引：扫描图库目录，重建数据库 | 无 |
| `gallery_import` | 导入新图片：扫描指定目录，搬图入库 | path（扫描路径）, rebuild（重建模式） |
| `gallery_search` | 搜索图片 + 单张详情 | keyword, tag/tags, matchAll, date_from, date_to, ext, limit, include_thumbnails, id |
| `gallery_tag` | 标签管理 | action(add/remove/list/search/rename/delete), image_ids, tag/tags, new_name |
| `gallery_config` | 查看/设置配置 | action(get/set), key, value |

**注意**：`gallery_info` 合并到 `gallery_search` 中，传 `id` 参数即返回单张详情。

### Phase 2 工具

| 工具名 | 功能 | 参数 |
|--------|------|------|
| `gallery_markdown` | 生成 Markdown 引用 ✅ | id, alt_text, size(raw/thumb), format(markdown/url/path) |
| `gallery_generate` | 生成画廊 HTML ✅ | tag, date_from, date_to, limit, output |

### Phase 3 工具

| 工具名 | 功能 | 参数 |
|--------|------|------|
| `gallery_export` | 导出索引 ✅ | format(json/sqlite), output |

---

## 六、配置项

```jsonc
{
  "galleryRoot": "",        // 图库根目录
  "scanPaths": [""],          // 扫描路径列表
  "thumbnailSize": 300,                         // 缩略图宽度 px
  "autoClassify": true,                         // 是否按 EXIF 日期自动分目录
  "defaultView": "date",                        // 默认搜索排序方式
  "blogImagesPath": "public/images/gallery",     // 博客项目中图片目录的相对路径；Markdown 引用从此路径推算
}
```

**路径对齐规则**：`blogImagesPath` 定义博客项目中图片目录的**文件系统相对路径**（如 `public/images/gallery`）。

Markdown 引用生成时自动处理路径转换：
- 文件系统路径：`{blogImagesPath}/{gallery相对路径}` → `public/images/gallery/2025/03/15/IMG_2847.jpg`
- Web 可访问 URL：自动去掉 `public/` 前缀 → `/images/gallery/2025/03/15/IMG_2847.jpg`

示例：
- 图片 gallery 路径：`2025/03/15/IMG_2847.jpg`
- blogImagesPath：`public/images/gallery`
- 生成的 Markdown：`![京都夜景](/images/gallery/2025/03/15/IMG_2847.jpg)`

博客项目可按需替换前缀（如换为 CDN 域名），图库只保证相对路径唯一且正确。

---

## 七、技术选型

| 项 | 选型 | 原因 |
|---|---|---|
| 数据库 | **sql.js**（WASM SQLite） | 零编译依赖，即装即用；防抖写入缓解性能问题 |
| EXIF 读取 | **exifr** | 纯 JS，Windows 上快 |
| 图片处理 | **sharp** | Node.js 最强，格式全（JPEG/PNG/WebP/AVIF） |
| 文件哈希 | **crypto (built-in)** | Node.js 内置，SHA-256 |
| 写入策略 | **防抖写入（debouncedSave）** | 500ms 延迟批量序列化，避免逐条全量写 |
| 画廊 HTML | ES 模板字符串 | 静态 HTML 够用，不引入框架 |

---

## 八、风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| EXIF 日期可靠性低 | 分类不准确 | 分级 fallback: EXIF > 文件修改时间 > 入库时间 |
| sql.js 全量序列化写回 | 大库写入慢 | 防抖写入（500ms）+ `flush()` 接口供关键操作即时写回 |
| sharp 预编译二进制缺失 | 缩略图功能不可用 | npm 缓存命中即可；降级用 CSS 缩放 |
| 文件重名 | 索引冲突 | SHA-256 文件指纹去重，同名文件自动加后缀 |
| Node.js ESM 缓存跨 reload | 开发时代码更新不生效 | 修改底层模块（db.js）后需重启 hana-server |
| EXIF Orientation 未处理 | 手机照片颠倒 | 缩略图生成时调用 sharp.rotate() 自动转正 |

---

## 九、开发约束（经验总结）

以下是在开发过程中发现的 Hanako 插件运行时约束：

1. **工具文件不能顶层 import 第三方 npm 包**
   - 只有 Node 内置模块（`fs`、`path`、`crypto` 等）可在工具文件顶层 `import`
   - 第三方包（`sql.js`、`sharp`、`exifr`）必须在 `execute()` 内用 `await import('pkg')` 动态加载
   - 违反此规则会导致工具注册静默失败

2. **`flush()` / `saveDb()` 必须兼容 ctx 参数缺失**
   - `runSql()` 内部调 `debouncedSave()` 不传 `ctx`
   - `flush()` 必须用 `ctx || _ctx` 做兜底，优先使用缓存上下文

3. **进程级 ESM 缓存**
   - Node.js ESM 模块按文件 URL 缓存，`plugin_dev_reload` 不清缓存
   - 修改 `lib/` 下的模块后需要重启 hana-server 进程

4. **路径转义**
   - Windows 路径中的 `\t`、`\n` 等在 JS 字符串中会被转义
   - 工具接收的路径参数使用正斜杠（`/`），代码存储时规范化

---

## 十、一些设计决策说明

1. **为什么 Phase 1 就做缩略图？**——一张缩略图生成几十毫秒，sharp 迟早要装。Phase 1 就做按需生成+缓存，体验差距很大，开发量几乎不影响。

2. **为什么标签要独立表而不是 SQLite JSON？**——爱莉丝说得对，手动打标意愿低，但标签如果存在独立表里，助手操作时查询效率差别很大。一张 SQL 就拿到"打了'京都'标签的所有图片"。

3. **数据库放在图库根目录而不是插件目录？**——图库数据可能很大（几十 GB），插件目录通常随软件装在 C 盘。`_index.db` 跟图片放一起，备份时一起打包。

4. **为什么初期不做轮询监听？**——简化。`gallery_import` 手动触发，等用顺手了再加自动监听。洛琪希的增量扫描方案不错，放到 Phase 3。

5. **为什么 Phase 2 先做 Markdown 引用，后做画廊？**——Markdown 引用是博客写作的刚需，每次写文章都可能用到。画廊是独立的展示需求，可以往后排。

6. **为什么搜索结果要返回缩略图路径？**——助手"看"不到原始图片，但可以读取缩略图路径并呈现给用户。这是"搜得到"的关键一环——搜索结果只有文件名的话，用户没法看图挑图。返回缩略图路径让助手能直接把图亮出来。

7. **为什么用了 sql.js 而不是 better-sqlite3？**——better-sqlite3 需要 node-gyp 编译（缺 CL.exe），sql.js 零编译即装即用。配合防抖写入策略，批量操作性能可接受。见"技术选型"节。

8. **为什么`gallery_rebuild` 是独立工具而不是 import 的子模式？**——开发中发现 async execute 中 `await import()` 会在运行时作用域切换，导致一些模块参数丢失。`gallery_rebuild` 使用内联实现（不依赖外部 lib 模块），避开了这个边界问题。

---

## 十一、开发速查

### Dev 循环

```bash
# 源文件修改后同步到 dev 插件
plugin_dev_reload
Set-Location <targetDir>
npm install        # dev 重装后 node_modules 被清空
plugin_dev_enable

# 如果修改了 lib/ 下的模块，需要重启 hana-server
```

### 快速部署（npm install 后）

```bash
# 在 dev 插件目录
Set-Location "C:\Users\<User>\.hanako\plugins-dev\hanako-gallery"
npm install
plugin_dev_enable
```

---

## 十二、打完收工

全部 4 个阶段已完成。工具代码就绪，待重启后注册到运行时验证。
总文件数：11 个工具 + 4 个 lib 模块 + 1 个入口 + 1 个页面 + manifest + skills = 20 个文件。
数据库：sql.js WASM，3 表 + 4 索引，防抖写入。
图片处理：sharp + exifr，SHA-256 去重，缩略图按需生成。
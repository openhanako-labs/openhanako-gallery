# Contributing Guidelines / 贡献指南

感谢你有兴趣为这个项目贡献力量！任何形式的贡献——代码、文档、问题反馈、建议——都欢迎。

## 快速开始

1. Fork 本仓库并克隆到本地。
2. 创建功能分支：`git checkout -b feat/your-feature`
3. 在分支上完成你的改动。
4. 提交前自测，确保功能正常、无明显回归。
5. 发起 Pull Request 到主分支，并在描述中说明改动内容与动机。

## 提交规范

- 提交信息使用简洁、描述性的写法，建议用 `type: 描述` 格式，例如：
  - `feat: 新增 XX 功能`
  - `fix: 修复 XX 问题`
  - `docs: 更新文档`
  - `refactor: 重构 XX 模块`
- 一次提交只做一件事，保持提交粒度清晰。

## 代码规范

- 遵循项目现有的代码风格与目录结构。
- 保持改动最小化，不引入无关的格式化或重构。
- 新增依赖前先确认必要性，尽量避免臃肿。

## 安全要求

- **绝不**将任何密钥、Token、Cookie、密码提交到代码或历史中。
- 涉及 API Key 的配置应使用 `.env.example` / `config.example.yaml` 等方式提供模板，真实密钥留在本地 `.gitignore` 忽略。
- 提交前检查暂存区，确保没有把本地敏感文件带进版本控制。

## 问题反馈

- 提交 Issue 时，请提供：复现步骤、期望行为、实际行为、环境信息（系统/版本）。
- 如果是 bug，尽量附上最小重现示例或日志片段。

## Pull Request 流程

- 每个 PR 绑定一个明确的目标或 Issue。
- PR 描述中说明改动背景与影响范围。
- 维护者会在评审后合并或给出修改意见。

---

*Copyright © 2026 OpenHanako Labs*